# Generated by main.go; the Pages workflow rebuilds it from source on every
# deploy, so it is build output, not something to commit.
/public/

# A stray `go build` here produces a binary named after the directory.
/site
